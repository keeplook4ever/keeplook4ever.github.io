baseURL = "https://keeplook4ever.github.io/"
title = "Lennon — Blog"
languageCode = "en-us"
enableRobotsTXT = true
theme = "PaperMod"

[pagination]
  pagerSize = 10

[params]
  env = "production"
  title = "Lennon — Blog"
  description = "Security, Humanity, Runner, Marathon, Music, Notes"
  keywords = ["security", "devsecops", "sdlc", "notes", "humanity"]
  author = "Lennon"
  defaultTheme = "auto"

  # 注意：PaperMod 参数名区分大小写，建议统一用小写的 showReadingTime
  showReadingTime = true
  ShowShareButtons = true
  ShowCodeCopyButtons = true
  ShowPostNavLinks = true

  # ✅ 正确的 profileMode 写法（是一个表，不是布尔）
  [params.profileMode]
    enabled = true
    title = "👋 Hi, I'm Lennon"
    subtitle = "Security engineer | Curious soul exploring technology and humanity / 科技与人文并重"
    # 可选：
    # imageUrl = "images/profile.jpg"
    # imageTitle = "Lennon"
    # imageWidth = 180
    # imageHeight = 180
    buttons = [
        { name = "About / 关于我", url = "/about/"}, 
	{ name = "Posts / 文章", url = "/posts/" }
    ]


  # 社交
  [[params.socialIcons]]
    name = "github"
    url = "https://github.com/keeplook4ever"

  # （可选）评论：PaperMod 推荐用嵌套表
  # 开启站点级评论总开关（可用在文章 front matter 里覆盖）
  comments = true

  # Giscus（示例；请用你的真实 repoId / categoryId）
  [params.giscus]
    repo = "keeplook4ever/keeplook4ever.github.io"
    repoID = "<REPO_ID>"
    category = "General"
    categoryID = "<CATEGORY_ID>"
    mapping = "pathname"
    reactionsEnabled = "1"
    emitMetadata = "0"
    theme = "preferred_color_scheme"

[outputs]
  home = ["HTML", "RSS", "JSON"]

[permalinks]
  posts = "/:year/:month/:slug/"

