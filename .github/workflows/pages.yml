name: Deploy Hugo to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: false   # ！！！！一定注意
          fetch-depth: 0

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: '0.147.4'
          extended: true

      - name: Show repo tree (content)
        run: |
          echo "== List content =="
          ls -lah content || true
          echo "== Show _index.md =="
          sed -n '1,80p' content/_index.md || true
          echo "== Show about.md =="
          sed -n '1,80p' content/about.md || true

      - name: Hugo lists
        run: |
          hugo env
          echo "== hugo list all =="
          hugo list all || true
          echo "== hugo list published =="
          hugo list published || true
          echo "== hugo list drafts =="
          hugo list drafts || true
          echo "== hugo list future =="
          hugo list future || true
 

      - name: Build site
        run: hugo --minify --cleanDestinationDir -D --baseURL "https://keeplook4ever.github.io/"

      # 🔎 调试用：查看构建结果
      - name: Show build output (for debug)
        run: |
          echo "== List public root =="
          ls -lah public || true
          echo "== List public/about =="
          ls -lah public/about || true

      # ✅ 确保 about 页面存在
      - name: Guard (ensure /about/index.html exists)
        run: |
          test -f public/about/index.html || (echo "ERROR: public/about/index.html not found"; exit 1)

      # 📦 上传静态文件工件
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

